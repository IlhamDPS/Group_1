<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidaudhitory.group_1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/weird_lanscape"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu" />
        <activity android:name=".halaman3content.tundra3minimenu" />
        <activity android:name=".halaman3content.gunung_kerinci" />
        <activity android:name=".halaman3content.galapagos" />
        <activity android:name=".tundra1" />
        <activity android:name=".tundra2"/>
    </application>

</manifest>